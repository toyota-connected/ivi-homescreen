name: mcp_drive_test
description: Integration test app for driving ivi-homescreen over MCP.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # The application-declared tools binding, from this repo.
  ihs_mcp_app_tools:
    path: ../../../packages/ihs_mcp_app_tools
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
